fragment HeroBlockPropertyData on HeroBlockProperty {
  HeroImage { ...ReferenceData }
  Icon
  Eyebrow
  Heading
  SubHeading
  Description { json, html }
  HeroColor
  HeroButton { ...ButtonBlockPropertyData }
}